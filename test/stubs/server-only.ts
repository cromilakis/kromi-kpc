/**
 * Stub vacío del paquete `server-only` para vitest (ver alias en
 * vitest.config.ts): el paquete real lanza al importarse fuera de React
 * Server, pero el guard que interesa lo aplica Next en build/runtime; en los
 * tests unitarios el módulo de scoring se importa directo.
 */
export {};
